services:
  account-hub:
    build:
      context: .
      dockerfile: Dockerfile
    image: account-hub:local
    restart: unless-stopped
    ports:
      - "8500:8500"
    volumes:
      - ./backend/data:/app/data
